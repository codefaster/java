package alg1;import de.beuth.algorithmen.storage.SortingAlgorithm;import de.beuth.algorithmen.storage.Storage;public class InsertionSort implements SortingAlgorithm {	public void sort(Storage s) {			for (int i=0; i<s.length();i++) {				int temp=s.get(i);	//Backup current element				for (int inner=0; inner<i; inner++) {            // inner -> i 					if (s.inRelation(s.get(inner), s.get(i))) {     // a[inner]<=a[i]						for (int g=i;g>inner;g--) {			    // 							s.set(g, s.get(g-1));						}					s.set(inner, temp);					}				}			}	}}